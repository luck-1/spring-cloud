spring:
  cloud:
    config:
      name: config_github_client
      label: master
      discovery:
        enabled: true
        service-id: configGithubServer

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/